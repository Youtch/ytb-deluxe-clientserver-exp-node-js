# Interpréteur utilisé
@Powershell OK node 22.17
@WSL        NOK

# Modes de lancements
A) node : lancement d'appli sur node OK `node main.js` ou `pnpm start`, packages node installés avec pnpm (registre unique)

B) deno : lancement d'appli sur deno NOK (ABANDONNE) `deno run .\\main-deno-adaptation.ts` => Test sur GitHub CF projet "..\\r\&d\_deno\_oauth\_github"
